import Context, { IContextOptions } from './context';
import { inspectCustomData } from '../../utils/constants';
export interface IVKAppPayloadPayload {
    user_id: number;
    app_id: number;
    payload: any;
    group_id: number;
}
export declare type VKAppPayloadContextOptions<S> = Omit<IContextOptions<IVKAppPayloadPayload, S>, 'type' | 'subTypes'>;
export default class VKAppPayloadContext<S = Record<string, any>> extends Context<IVKAppPayloadPayload, S> {
    constructor(options: VKAppPayloadContextOptions<S>);
    /**
     * Returns the identifier of the user whose action the event was sent to in the application
     */
    get userId(): number;
    /**
     * Returns the identifier of the application from which the event was sent
     */
    get appId(): number;
    /**
     * Returns the identifier of the community to which the notification was sent
     */
    get groupId(): number;
    /**
     * Returns the transferred useful data
     */
    get eventPayload(): number;
    /**
     * Returns the custom data
     */
    [inspectCustomData](): object;
}
