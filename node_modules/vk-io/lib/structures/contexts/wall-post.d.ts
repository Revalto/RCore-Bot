import Context, { IContextOptions } from './context';
import { WallAttachment } from '../attachments';
import { inspectCustomData } from '../../utils/constants';
export interface IWallPostContextPayload {
    id: number;
}
export declare type WallPostContextOptions<S> = Omit<IContextOptions<IWallPostContextPayload, S>, 'type' | 'subTypes'>;
export default class WallPostContext<S = Record<string, any>> extends Context<IWallPostContextPayload, S> {
    wall: WallAttachment;
    constructor(options: WallPostContextOptions<S>);
    /**
     * Checks is repost
     */
    get isRepost(): boolean;
    /**
     * Removes a record from the wall
     */
    deletePost(): Promise<number>;
    /**
     * Returns the custom data
     */
    [inspectCustomData](): object;
}
