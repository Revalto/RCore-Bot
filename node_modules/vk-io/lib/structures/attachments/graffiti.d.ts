import VK from '../../vk';
import Attachment from './attachment';
import { inspectCustomData } from '../../utils/constants';
export interface IGraffitiAttachmentPayload {
    id: number;
    owner_id: number;
    access_key?: string;
    height?: number;
    width?: number;
    url?: string;
}
export default class GraffitiAttachment extends Attachment<IGraffitiAttachmentPayload> {
    /**
     * Constructor
     */
    constructor(payload: IGraffitiAttachmentPayload, vk?: VK);
    /**
     * Load attachment payload
     */
    loadAttachmentPayload(): Promise<void>;
    /**
     * Returns the graffiti height
     */
    get height(): number | null;
    /**
     * Returns the graffiti width
     */
    get width(): number | null;
    /**
     * Returns the URL of the document
     */
    get url(): string | null;
    /**
     * Returns the custom data
     */
    [inspectCustomData](): object;
}
