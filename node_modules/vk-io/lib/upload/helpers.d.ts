/// <reference types="node" />
import { Readable } from 'stream';
/**
 * Check object is stream
 */
export declare const isStream: (source: string | Readable | Buffer) => boolean;
/**
 * Copies object params to new object
 */
export declare const copyParams: <T, K extends keyof T>(params: T, properties: K[]) => Pick<T, K>;
/**
 * Returns buffer from stream in Promise
 */
export declare const streamToBuffer: (stream: Readable) => Promise<Buffer>;
